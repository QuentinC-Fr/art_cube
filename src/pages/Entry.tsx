import CityForm from "../components/inputs/CityForm";

const Entry = () => {
  return (
    <div className="App">
      <header className="App-header">
        <h2>Entry</h2>
        <CityForm />
      </header>
    </div>
  );
};

export default Entry;
